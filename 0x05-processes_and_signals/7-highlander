#!/usr/bin/env bash
# Script to stop the 7-highlander process without using killall

pid=$(pgrep -f 7-highlander | head -n 1)

if [ -n "$pid" ]; then
  # Send SIGTERM using /proc instead of kill or killall
  echo > "/proc/$pid/fd/0"
fi
