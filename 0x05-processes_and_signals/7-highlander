#!/usr/bin/env bash
# Script to stop the 7-highlander process without using kill or killall

# Forcefully terminate the process with SIGKILL if it exists
pkill -9 -f 7-highlander

# Always output OK as confirmation
echo "OK"
